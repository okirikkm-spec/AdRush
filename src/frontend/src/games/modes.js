import { BracketIcon, CrownIcon } from "../components/icons";

/**
 * Каталог режимов мини-игр — общий источник и для хаба /games, и для шапок самих игр.
 * Добавить режим = дописать сюда объект и повесить его path на страницу в App.js
 * (а также в список SPA-маршрутов на бэкенде, см. SpaController).
 */
export const GAME_MODES = [
  {
    id: "king",
    path: "/games/king",
    title: "Царь горы",
    icon: CrownIcon,
    description:
      "Слева - действующий царь, справа - претендент. Выбираете того, кто нравится больше: " +
      "победитель остаётся на горе и встречает следующего, проигравший выбывает.",
  },
  {
    id: "bracket",
    path: "/games/bracket",
    title: "Турнирная сетка",
    icon: BracketIcon,
    description:
      "Сначала все банки разбиваются на пары: первые две, потом другие две и так до конца списка. " +
      "Когда участники кончатся, победители сходятся между собой в том же формате - и так до финала.",
  },
];

export const findMode = (id) => GAME_MODES.find((m) => m.id === id) || null;
